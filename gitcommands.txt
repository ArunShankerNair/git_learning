git clone "remote path"

git push "origin branch"

git pull : remote from changes in remote repository to local repository

git init : for initialization of empty git repository

git add -A

git commit -m "message"